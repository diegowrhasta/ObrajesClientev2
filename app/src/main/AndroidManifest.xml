<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mateo.obrajescliente">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Sayt'awiwi"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Bienvenido"
            android:label="@string/title_activity_bienvenido"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".RegistroUser" />
        <activity android:name=".Qr" />
        <activity android:name=".Calle2a"></activity>
        <activity android:name=".Calle2b"></activity>
    </application>

</manifest>